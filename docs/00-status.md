`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# 0. Status and how to resume (updated 2026-09-23)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |The handoff for a new session: the tracks and what each owns, where each
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |area stands, the everyday commands, open threads, next steps and the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |gotchas that cost a day each. Current state only; a fixed thing moves to
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |its design doc and the commit log. Decisions are in doc 10, the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |milestone plan in doc 08, test tools in `docs/testing.md`, build stages,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |player options and logs in `docs/development.md`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Tracks (pick one per session)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Work runs as parallel tracks, one session each. Each track doc has its
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |scope, owned files, state, test loop and ordered next steps. This table
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |is the index.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Track | Doc | Owns | State · next |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer ||---|---|---|---|
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M4** paravirtual Direct3D device | `tracks/m4-d3d-device.md` | `d3dpt/exec/`, `d3dpt/hw/d3dpt_mm.c`, `guest-tools/src/d3dpt/`, `scripts/test.sh`, doc 14 | Done; the Win98 DLL path and the executor's harness · a game by hand, the P8 and other stubs, zero-copy present |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M5** CD-ROM backend | `tracks/m5-cdrom-backend.md` | `libdisc/`, patches 50–59, `tools/atapi-guest-test.py`, `guest-tools/src/cdtest.c`, docs 05, 17 | Done (steps 1–8) · FIFA 2002's no-match, a second SafeDisc 2 title, SecuROM, multisession, CHD |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M5g** a host folder as a CD (`isodir:`) | `tracks/m5-dirdisc.md` | `libdisc/src/isodir.rs`, `libdisc/qemu/cdimage.c` | Done |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M6** launcher and packaging | `tracks/m6-launcher.md` | `launcher-core/`, `launcher-qt/`, `launcher-capi/`, `shader-chain/`, `scripts/package-*.sh`, doc 07 | Shipped (Qt, ADR-015/017), continues on `main` · AppImage, Windows installer, the preview as a `QQuickRhiItem`, the player's own overlay controls |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M7** XP display driver | `tracks/m7-display-driver.md` | `d3dpt/hw/d3dpt_vga.c`, `d3dpt/hw/d3dpt_exec_load.[ch]`, `d3dpt/d3dpt_fb.h`, `d3dpt/exec/d3dpt_exec_ddi.cpp`, `guest-tools/src/d3dptvid/nt/`, `tools/xp-*.sh`, `tools/d3dpt-dp2-test.cpp`, doc 15 | Done through protocol v13 · a title for each probe-only DX8 feature, more 8 bpp titles, a driver stage in `scripts/test.sh` |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M8** x87 / SSE fast paths | `tracks/m8-tcg-fastpaths.md` | patches 05, 06, 11, 12, `tools/x87-*`, `tools/sse-guest-test.py`, docs 13, 16 | Done · a real Direct3D workload with and without `*-fast=off` |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M9** TCG on Apple Silicon | `tracks/m9-tcg-aarch64.md` | `tools/tcg-profile.*`, `tools/tcg-hot.py`, the TCG patches from 13 on | Done; optimization closed by user decision (2026-09-12) · patch 21's crash, binary32 at PC=24 slower than PC=53 on aarch64, the game tests uncapped on the Air |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M10** Win98 display driver | `tracks/m10-win98-driver.md` | `guest-tools/src/d3dptvid/core/` and `w9x/`, `guest-tools/build-driver*.sh`, `setup.c`'s 9x role, `tools/win98-*.sh`, doc 19, ADR-012 | Active; steps 0–4 done, step 5 (real titles) under way · the ACPI standby resume |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M11** Windows host | `tracks/m11-windows-host.md` | `packaging/windows/`, `scripts/win-cross.sh`, `build-windows.sh`, `package-windows.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M12** music | `tracks/m12-music.md` | `libsynth/`, patches 60–61, `soundfonts/`, `bundle::Sound` / `Music`, `tools/midi-guest-test.py`, doc 20 | All stages landed · capture Win98's failing MIDI run, a host MIDI port |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M13** gamepads | `tracks/m13-gamepads.md` | `player/src/pad.rs`, `gamepad/`, patches 26–27, `bundle::Pad`, `tools/pad-guest-test.py` | Done · a real controller on the key mapping, the USB pad on Win98 FE / Me |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M14** Voodoo 2 device | `tracks/m14-voodoo2.md` | `voodoo/`, patch 62 and the Voodoo patches after it, `tools/voodoo-guest-test.py`, `scripts/sync-86box-voodoo.sh`, doc 21 | Active on `main` · a second Glide game after one has quit, a client left on a dead ring, the Air and Windows builds |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || **M15** Direct3D executor on Wine | `tracks/m15-wine-executor.md` | `d3dpt/exec/d3dpt_exec_host.c`, `d3dpt_exec_remote.c`, `d3dpt_remote.h`, the loader's library choice, `build-d3dpt-exec.sh --wine`, `player/src/companions.rs`, `launcher-core/src/host_gpu.rs` | Steps 1–6 done: the community app passed on a real macOS 15 and WineD3D-in-guest was removed (2026-09-23) · step 7, the Flatpak's Wine add-on (decided 2026-09-23, an extension of the app); the spike's host tests on the rig's Linux Wine |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Everything else (Glide on macOS / Windows, M2's leftovers) | "Next steps" below | | as listed |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Rules: work on `main` or on a branch `track/<name>-<topic>` off it,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |rebased on `main` before pushing and merged when green. Edit shared files
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |(`d3dpt/d3dpt_proto.h`, `d3dpt/exec/`, `scripts/test.sh`, `player/`,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |`CLAUDE.md`, this doc) minimally and name the track in the commit
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |message. In this doc a track edits only its own row here, its "Where
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |things stand" row and its lines under "Next steps"; everything else goes
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |in its track doc. A merged track's branch and worktree are deleted.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Branches still on the remote: `track/m10-win98-driver` and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |`voodoo2-mmio-holes` (merged), `track/m9-hwmmu` (the hardware-MMU gauge,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |parked), `m14-glide3` (abandoned, tagged `m14-glide3-abandoned`) and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |`track/m14-voodoo2-sli` (an SLI pair and the Voodoo 3's screen filter,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |unmerged). The Mac pulls `main`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Where things stand
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Area | State |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer ||---|---|
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || QEMU | v9.2.4 + qemu-3dfx (`d00e858`) + our patches 01–73 (`patches/qemu/README.md`). Built without display, host-audio, extra network or network-block backends (the `no-optionals` check). Windows QEMU is built with clang (patch 68); the Mac build needs no XQuartz (patch 70). |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Emulated CPU (TCG) | x87 shadows at PC=24/53/64 (doc 13), SSE and SIMD inline (doc 16), the M9 queue (REP, same-value SMC, soft immediates, inline TB lookup, TLB work). Default is 2.34x geomean over pristine 9.2.4 on the Air, all switches off 0.97x (doc 22). Every patch has an off switch in the machine form except `pinned-regs` (patch 21, doc 18), not offered: it crashes XP. The hardware-MMU design (1.1–1.2x) is parked (user decision, 2026-09-16). |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Player | QEMU in-process (`libqemu-embed`, embed API v8), wgpu + librashader CRT chain, mode analysis (doc 03), the embed audiodev (f32, paced to the guest's clock, limiter; doc 11), guest cursor as the window cursor, gamepads, host modifier keys, Alt+F4 asks. Options: `docs/development.md`. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || OpenGL / Glide pass-through | In the player on Linux (EGL), macOS (CGL) and Windows (WGL, doc 12 "The WGL rule"). Zero-copy: dma-buf ring on Linux (repairs a slot that stops being written through), IOSurface on macOS. Glide 2 through our OpenGLide build (doc 12 §5); game evidence is headless only (Rayman 2, Carmageddon DOS). No Windows Glide wrapper. Glide 3 wrapper abandoned for the Voodoo 2 device. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Voodoo 2 (doc 21) | `-device voodoo2`, 86Box's chip. 3dfx's own Win98 driver runs Quake II, UT and NFS Porsche (the user, by hand). FIFA 2000's and Carmageddon's FIFO hangs fixed (doc 21 §11, §13). 8 MB board by default (`texmem=2`), command FIFO in guest RAM (`ramfifo=on`, Quake II 41 → 147.5 fps). Open: a second game after one quits sometimes starts glitched. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Direct3D executor (doc 14) | Protocol v13, one decoder, four D3D9s. DXVK (native and on Windows) is the default and the golden reference. Below the Vulkan 1.3 floor: Windows' own `d3d9.dll` (`D3DPT_D3D9`, ADR-007's second amendment) or Wine's on a Linux / macOS host (`exec=wine`, ADR-018, M15). `no-exec=on` models a host with no executor. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || XP display driver (doc 15) | `d3dpt-vga`, register set v5. DirectDraw and a DirectX 8 DDI with hardware T&L, shaders 1.x, palettes, colour keys, VRAM buffers, 16 streams, cube / volume textures, MSAA, gamma. FIFA 2000, Max Payne, Vice City, Moto Racer and Diablo play. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Win98 display driver (doc 19) | The same core under a 9x layer; the default adapter for a new Win98 machine. The DirectX 3–8 checks pass as on XP, and a 2ksbox Win98 runs DirectX 9.0c. Crimson Skies, 3DMark 99 / 2001 SE, Carmageddon (Mode X), Blood in a DOS box. Blue screens and power-down show. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || CD-ROM (docs 05, 17) | `libdisc` behind the `cdimage` driver: cue/bin, CCD, MDS, ISO and `isodir:` folders, L-EC, subchannel, CD-DA, a DVD profile past 80 minutes, the disc shelf from inside the guest (patch 52, `CDSHELF`; the listing names the disc in the drive from the medium itself, boot disc included). SafeDisc 1.x's band read is the negative control; SafeDisc 2.x and ProtectCD never read theirs. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Music (doc 20) | OPL3 and MPU-401 (no IRQ line) on SoundFont GM or the user's MT-32 ROMs. The SB16 applies its mixer (patch 61). Open: Win98's own MIDI through our port loses instruments. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Gamepads (M13) | USB HID pad (patch 26), gameport (patch 27), key mapping. Done. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Guest machines (doc 06) | Four families: Win98, XP, DOS, Other. Win98 / XP start on `d3dpt-vga`, DOS / Other on `std`. No network card by default. Win98 is TCG with `hpet=off`; the BIOS date stamp makes it install ACPI. DOS paces with `-icount …,align=on`. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Guest tools (`guest-tools/README.md`) | One ISO. `SETUP.EXE` installs what this Windows can use; every program logs to `C:\2KSBOX` (`BOXLOG=` overrides). |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Launcher (doc 07) | `launcher-qt` over `launcher-core` (also `launcherx`, `launcher-capi`). The machine form is a settings window, a page per section, every picker the style's own combo box over the model's rows; the Direct3D picker shows only what this host runs. Extra QEMU arguments, clone, first-run preset download. The snapshot window is a tree (2026-09-23): a qcow2 records no parent, so the launcher writes each take and restore to `snapshots.toml` beside the bundle and reconciles it against the disk on every read; snapshots it has no record of sit at the top level. Window text is short and plain (user rule). |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Packages | Linux tarball, Flatpak (`org.kde.Platform` 6.10), macOS app in two builds (ADR-019: App Store 26+, community with the Wine pair at Homebrew's floor, 15.0), Windows zip (cross build; native MSYS2 build for debugging) and, from 2026-09-23, the same tree as a Microsoft Store MSIX (`scripts/package-msix.sh`, packed on the PC; not yet uploaded, `build-windows.md` "The Store package"). Every packager opens a real window offscreen. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Tests | `scripts/test.sh host` (~30 s) / `all` (+ XP and DOS guests). Integration only, local only; `docs/testing.md`. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer || Guest images | Outside the repo, read-only for a session: `~/vms/win98.qcow2`, `winxp.qcow2`, `winxp-m7*.qcow2`, `scratch.img` (E: in XP), and the launcher library's machines (`~/.local/share/2ksbox/machines/`: `base98-br`, `base98-us`, `claude98`, `win98-2`, …). Boot an overlay or a copy. |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Build / run cheat sheet
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Stages, player options and packagers: `docs/development.md`. Test tools:
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |`docs/testing.md`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |```sh
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |scripts/build.sh           # after every pull: everything, only what changed
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |                           # (-f re-runs every prepare, --test adds test.sh host)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |scripts/test.sh            # host stage; `all` adds the guests (before any
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |                           # commit touching QEMU, embed, the D3D device, guest DLLs)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# a machine the way the launcher runs it
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |target/release/launcherx --print-args <machine>/machine.toml   # the exact QEMU line
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |target/release/player --shader third_party/slang-shaders/crt/crt-lottes.slangp -- \
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  -L $PWD/qemu/pc-bios -machine pc,hpet=off -cpu pentium3 -m 256 \
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  -hda <overlay>.qcow2 -vga none -device d3dpt-vga -usb -device usb-tablet \
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  -device sb16,audiodev=embed0 -qmp unix:/tmp/q.sock,server,nowait
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# driving a guest
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |tools/qmpc.py /tmp/q.sock keys meta_l+r      # Run dialog; `type '…'`, `keys ret`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |tools/qmpc.py /tmp/q.sock json '{"execute":"system_powerdown"}'   # clean stop
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |mcopy -i ~/vms/scratch.img@@1048576 ::/OUT/G9.BMP g9.bmp   # a file out of E:
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# host-only D3D checks, and the XP driver loop
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |build/d3dpt-exec-test x.bmp 120 60
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |build/d3dpt-dp2-test x.bmp
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |tools/xp-driver-test.sh <overlay> d3d7        # NO_EXEC=1 / EXEC=wine for the fallbacks
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# CD images
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |target/release/discx scan game.cue            # the bad-sector map of a dump
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |CDIMAGE_TRACE=1 build/qemu/qemu-system-i386 … -cdrom game.cue   # every ATAPI packet
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |# a disc with CD audio in the player: the explicit drive, on its own channel
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |#   -drive if=none,id=cd0,media=cdrom,file=game.cue \
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |#   -device ide-cd,bus=ide.1,id=ide1-cd0,drive=cd0,audiodev=embed0
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |```
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |A bare `qemu-system-i386` needs `-L qemu/pc-bios` by hand (its other
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |traps are under "Driving a guest headless"). The DOS batteries fetch the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |FreeDOS floppy themselves; on a Mac they need `brew install nasm
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |mtools`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Open threads
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Unfinished or unexplained things across tracks. A track's own open items
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |live in its track doc; fixed things leave this list.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **GL and Glide on a Windows host.** The OpenGL pass-through runs there
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`GLPROBE.EXE` in `base98-br` reads the host's renderer since the WGL
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  fix, doc 12 "The WGL rule"), but no game has run on it. GLQuake is the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  user's next try. The Glide wrapper has no Windows build (doc 12
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  "Order"; the package lists `glide` as "(not shipped)").
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Windows' own Direct3D 9 is unproved on the hosts it is for.** Both
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  oracles match byte for byte on both backends, but only on an RTX 3090
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (ADR-007's second amendment). Expect gaps in pre-Broadwell Intel,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Kepler and TeraScale drivers; next is a real title on such a host with
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `D3DPT_D3D9=system`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Win98's ACPI standby does not come back** (doc 19 §41). Standby
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  suspends the VM, input piles up in the embed queue (512 events, 151
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  dropped, 20 s late on the user's run), and on wake nothing reprograms
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the adapter, so a blank text page idles back into standby.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **The zero-copy ring's frozen slot has no known cause.** `zc_probe()`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  repairs it; doc 12 §4 has what was ruled out and the suspects left.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **3DMark 99 on the Windows PC: two threads** (M14/M11, `base98-br`,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `scripts/win-voodoo-ab.sh`, log `build/win-voodoo-ab.log`; the FIFO
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  hangs are fixed, doc 21 §9).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  - *A garbled loading screen*, seen once, usually the Fill Rate one. It
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    is the 800x600 desktop on `d3dpt-vga` in stale bands, not a Voodoo
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    frame. `vga:full-frames=on` does not change it, and no flips or
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    executor batches run meanwhile, so the guest wrote those bytes. Next:
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    which blit draws that background and where it reads from.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  - *The whole machine 3x slower after some guest restarts*, with no
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    Voodoo (`no-voodoo`): 46.3, 46.7, 62.4, **15.0**, 37.8 fps across
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    restarts in one player run, everything slower by the same factor. Not
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    a context leak, the ring falling back to MMIO, or audio or input
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    stalls. `d3dpt-vga` reports `N batches in 5.0 s, M ms of them in the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    executor`, and the script passes `-msg timestamp=on`. A flat host
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    share with the rate halved points at the guest or the vCPU. Also
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    check inside Windows (Performance tab: a file system not "32-bit"
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    after hard resets) and the host's CPU use.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **`GetSwapChain` is a stub** in the guest D3D9 DLL (doc 14, "A review of
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the guest DLLs").
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **The NT side of the 3DMark2001 fixes is not re-run** (doc 19 §38):
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `xp-driver-test.sh install` installed no driver on a `winxp-m7`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  overlay (HEAD's and the previous driver alike), so BUMPTEST on XP is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  unmeasured. It matches the DRVINST Logo-dialog watcher bug fixed since;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  re-run with a current DRVINST.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A fault inside a DDI callback leaks the command-window lock** and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  freezes the session until the process dies (doc 19 §36). Accepted for
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  v1 (user decision, 2026-09-16). The fix is an exception frame that
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  releases the lock on unwind.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Win98 `SETUP /ALL` over an installed driver: `WININIT.INI [rename]`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  sometimes lacks the `SYSTEM\` entries** (`VOODOO=1
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  tools/setup-guest-test.sh` on `~/vms/win98.qcow2`). All seven copies
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  are staged and logged, but the INI read right after holds the four
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `INF\` renames and zero or one of the three `SYSTEM\` ones. Unknown
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  whether the 9x profile cache had not flushed yet (`REBOOT=1` shows if
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the restart still applies them) or the renames are lost. That image's
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `SYSTEM\GLIDE*.DLL` and `FXMEMMAP.VXD` are read-only leftovers, so its
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  three Voodoo marker checks fail regardless.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **On the Cirrus, a VESA picture comes out in swapped blocks** (the user,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  in Duke Nukem 3D). The colours were the missing 4F09h, now fixed. Ruled
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  out:
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  - The chain-4 bug: in VBE modes both adapters map 0xA0000 as a RAM
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    alias, so writes never reach `vga_mem_writeb`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  - The window granularity: the Cirrus's 16 KiB is the hardware's, and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    `tools/vga-dirty-guest-test.py vesa cirrus` passes (`GRAN64=1`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    assumes 64 KiB, as the A/B).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Untested hypothesis: Build's own Cirrus SVGA driver banks through
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  GR9/GRB directly and may disagree with `cirrus_update_bank_ptr` about
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  GR0B bit 5. Next: a test that drives those registers both ways round,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  or a log of the game's register writes.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Protected discs against the EDC-first cooked read: argued, not
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  measured.** Re-run `discx scan` on the rig's protected dumps
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`docs/tracks/m5-cdrom-backend.md` has the checks, doc 17 §2.5;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `LIBDISC_NO_CORRECT=1` is the A/B).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **The CD-ROM drive has no speed model.** It advertises 4x, `SET CD
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  SPEED` does nothing, and a whole-disc read runs at hundreds of MB/s.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `throttling.bps-read=` holds back a `.iso` but not a libdisc image.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `tools/cd-rate-guest-test.py` shows the bytes do not depend on the read
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  rate. Untested: whether a title that paces itself on CD reads minds a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  drive 100 times too fast. That needs a `speed=` on `ide-cd` both
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  drivers honour (`docs/tracks/m5-cdrom-backend.md`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Below the Vulkan floor: no real-user numbers.** Nobody has measured
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  how many users are below DXVK's Vulkan 1.3 bar, or whether on such a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  host software Vulkan (lavapipe, "available, in software (slow)") beats
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the Wine executor (ADR-013/018, `launcherx --host-check`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **3D hand-off sync is `glFinish`** on both platforms. A fence would let
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the vCPU go on while the blit drains (doc 12).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Next steps, in order
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Each track's own order is in its track doc. This is the order across
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |tracks, plus the items no track owns.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |1. **M15, the Direct3D fallback on Wine** (ADR-018,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   `tracks/m15-wine-executor.md` "Steps"). Steps 1–6 are done: the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   packaged community app on a real macOS 15 (the floor, `build-macos.md`)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   runs the game through the Wine executor, user-confirmed on 2026-09-23
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   ("works wonderfully"; no frame rate was written down), and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   WineD3D-in-guest was removed the same day. Left: step 7, the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   Flatpak's Wine as an add-on extension of the app (decided 2026-09-23;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   the shape is in the track doc), and the spike's two host tests on the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   rig's Linux Wine. A Windows host below the floor is not part of this;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   it already runs its own `system32\d3d9.dll`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |2. **The measurements doc 22 still owes** (user decision, 2026-09-15).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   The Ryzen half of §6.2's games, including 3DMark2001 SE's high-detail
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   Car Chase and Lobby as the benchmark for patch 47's inexact mode (+47 %
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   and +15 % on the Air). Helper reach on x86-64, where it is the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   *always* case (`call [rip+pool]` from a PIE or a Windows EXE): a Linux
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   / Win32 variant of patch 63 as the A/B through `tools/specbench`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   Three QEMU builds from one tree behind a meson option (switches
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   removed / hardwired on / switchable) to price the switches; later,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   "all off plus one switch". Not worth chasing: `bl` reach on the Mac
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   (2 %).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |3. **Patch 21 (`pinned-regs`)** crashes XP under Super PI with seven or
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   eight registers pinned (a bugcheck with auto-restart;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   `tools/specbench/run.sh <image> pinned` reproduces it; doc 18 open
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   item 1). It is off and not in the machine form (user decision,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   2026-09-16: 1.1–1.2x at best), so fixing it is optional.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |4. **Glide pass-through (M3, doc 12 §5).** The user has never had it
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   work by hand, so a hand run of a Glide title is owed; no Glide game has
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   run on the DOS family. Then a macOS `glide-host` check (the CGL side of
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   `tools/glide-host-test.cpp`) and a Glide guest on the Air, a Windows
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   Glide wrapper (M11's cross build has no stage), and fence-based sync
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   instead of `glFinish`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |5. **Display (M2, doc 03).** An answer for presets with no resolution
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   override. XP's mode table fed from the player and a present
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   signal in phase with its swapchain (M7). The player's own overlay
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   controls (pause, snapshot, disc swap; doc 07).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |6. **Windows host (M11's leftovers).** Moto Racer's speed on the PC
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   (CPU-bound, not reproduced on Linux; M11 track doc) and the first
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   clang-built QEMU there. The native MSYS2 build run (`scripts/win-run.sh
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   launcher`, a machine, the Windows-built ISO in a guest). Live control
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   over Winsock AF_UNIX on a real PC. The installer. Zero-copy frames
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   through a DXGI shared handle. A Windows check that boots a guest.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |7. **M14, Voodoo 2** (its track doc, "Open, in order"): the glitched
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   second Glide game, a client resuming on a dead ring, DxDiag's
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   Direct3D 7 `GetDC` failure, the Air and the Windows build, Diablo II's
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   numbers, patches 64 and 71 upstream.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |8. **M10, Win98 driver** (its track doc, "Next steps"): the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   command-window lock (§36), the ACPI standby resume (§41).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |9. **M12, music.** One dxdiag music run on Win98 with
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   `LIBSYNTH_MIDI_LOG` and `LIBSYNTH_OPL_LOG` set (doc 20 §7.2). Then
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   "MPU-401 Compatible" from Add New Hardware, the step Win98 needs
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |   before it plays MIDI to the port, and a host MIDI port (doc 20 §8).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |10. **M6, launcher and packages.** An AppImage (6b′), the Windows
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    installer (6d), the shader preview as a `QQuickRhiItem` (doc 07),
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    screenshots for a Flathub submission, `CDSHELF.EXE`'s Win98 (ASPI)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    run.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |11. **M5, CD-ROM.** Triage FIFA 2002's no-match. Age of Mythology disc 1
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    as a second SafeDisc 2 title. SecuROM (needs DPM in `mds.rs`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    Multisession. CHD. Win98's CD Player by ear. M5g: a guest-side check
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    of the stale-file rule.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |12. **The finished tracks' leftovers.** M4: a D3D8/9 game by hand on the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    DLL path, its stubs, a decoder thread. M7: a shader title, a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    split-stream title, 3DMark2001's Nature for cubes, StarCraft / Age of
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    Empires on 8 bpp, a driver stage in `scripts/test.sh`, something
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    better than a black screen when a guest's driver refuses the adapter.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    M8: a Direct3D title with and without `*-fast=off`. M9: the Air's game
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    tests uncapped (`DDFLAGS=32768`), binary32 at PC=24 on aarch64 (0.49 s
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |    against PC=53's 0.38 s on the Air, not profiled).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |## Gotchas
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |Cross-cutting traps, each as symptom → cause → rule. A trap that belongs
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |to one subsystem lives in its design doc; pointers are at the end.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Building
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Two builds must never share the `qemu/` tree at once.**
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `build-windows.sh` re-applies the patch queue while
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `package-flatpak.sh` copies the tree, and the copy fails deep in the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  compile with a header neither build uses. The outputs (`build/qemu`,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `build/win/qemu`) are separate; the sources are not.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A build belongs to one checkout.** Never point `QEMU_BIN` or any
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `*_BIN` at another checkout's artefacts, configure into its `build/`,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  or run its scripts. That tests someone else's patch queue, and meson's
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  recorded source path makes the other build compile *your* sources from
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  then on (check `build/qemu/meson-logs/`' "Source dir"). Moving a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  checkout invalidates `build/` too: `scripts/build.sh -f`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A source edit with no effect: is its directory in the stamp?**
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `scripts/build.sh` skips `prepare-qemu.sh` when the hashed inputs of
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `stamp_stale qemu-prepare …` are unchanged, so an overlay missing from
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  that list rebuilds the *old* file silently (`libsynth/qemu` was). `-f`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  bypasses every stamp.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Removing a patch to A/B it leaves its edits behind.**
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `prepare-qemu.sh` restores only files a *current* patch touches, so a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  dropped patch's changes to other files, and its new files, survive.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Use the patch's off switch if it has one; otherwise `git checkout`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  those files in `qemu/` and re-prepare.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **`configure`: "found no usable distlib".** pip 26 vendors
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `distlib.scripts` but not `distlib.version`, which QEMU 9.2's `mkvenv`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  imports. Install the real `distlib` for that interpreter. Python is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  uv's 3.12; 3.14 works only with the real `distlib` (MSYS2).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An ISO older than its sources means a stage died.**
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `build-wrappers.sh` is `set -e` and writes the ISO last. On a Mac,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Homebrew's mingw is a symlink, so `build-driver.sh` finds the DDK
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  headers through `-print-sysroot`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Guest binaries are msvcrt and `-march=pentium3`.** Modern mingw links
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the UCRT, which 9x lacks and XP never loads, and qemu-3dfx compiles for
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `x86-64-v2`; the scripts force and check both. Define `PSAPI_VERSION
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  1`, or `psapi.h` binds Windows 7's `K32*` exports and XP's loader stops
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the process in a hard-error box before `DllMain`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Host toolchain.** QEMU 9.2 needs `--disable-werror`, `-fPIC` and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `b_staticpic` for the shared library. On macOS every stage targets
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Homebrew's floor (`scripts/macos-floor.sh`, 15.0 today); a hand-run
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  cargo needs `MACOSX_DEPLOYMENT_TARGET` exported, and a `cargo clean`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  after the floor rises (`build.sh` does both). The macOS link needs
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `qemu_default_main` (defined in `embed/libqemu_embed.c`) and our ld64
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  export list.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Windows: `Unable to create index.lock: File exists`, the lock gone
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  when you look.** A scanner holds the lock of the git that just exited.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `prepare-qemu.sh` runs every git through `qgit`, which retries on that
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  and nothing else. A retrying wrapper must pass git's stdout through
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`2>&1 >&3` inside `{ } 3>&1`) or `ls-files` returns nothing.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Running on a Mac
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **`/opt/homebrew/lib` on `DYLD_LIBRARY_PATH` kills every image
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  decode** (`SIGBUS` at `0xbad4007` in `IIO_Reader_GIF`). dyld searches
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  it by leaf name first, and on a case-insensitive disk it answers
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  ImageIO's `libGIF` / `libPng` / `libTIFF` / `libJPEG` with Homebrew's.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Put only `/opt/homebrew/opt/vulkan-loader/lib` there; unsetting it at
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  run time is too late. `DYLD_PRINT_LIBRARIES=1` shows it.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A benchmark a third slower than the last is a far launch.** TCG's
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  code buffer 8 GiB from the helpers turns every helper call into
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `movz/movk ×4 + blr` (x87 / SSE helpers at 0.55–0.65x). Patch 63
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  reserves it near the image at load time. Check the JIT addresses in a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `sample` before believing a difference, and use
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `build/specbench/noaslr` for runs that must repeat (doc 22 §5.0).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A DXVK program's memory is its peak footprint** (`/usr/bin/time
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  -l`), not RSS, because GPU memory is the same RAM. SIP strips `DYLD_*`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  at every system binary, so put `env DYLD_LIBRARY_PATH=…` last in a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  wrapper chain. A producer that never waits outruns DXVK's deferred
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  frees (a 16 GB Mac swapped for minutes; doc 14).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Never call `gl*` / `CGL*` / `IOSurface*` by link in the embed
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  backend.** The symbol can bind to a GLX library that silently no-ops;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `dlsym` from the OpenGL.framework handle.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### The player and the QEMU thread
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Never `exit()` while the QEMU thread is alive.** QEMU's atexit
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  handlers race `qemu_cleanup` (`mutex->initialized` on macOS). The
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  player joins the thread; headless paths use `_exit`. A guest power-off
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  ends the loop while the UI still holds the handle, hence the stop /
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  release handshake before `qemu_embed_destroy`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An occluded window gets no swapchain image.** Per-frame work that
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  must not stall (importing zero-copy slots) runs on the wake event.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A crackle report: ask for the lines first.** `[audio] device asks for
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  N frames`, `qemu-embed: audio:` and `[audio] the guest's mix went past
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  full scale`. A timing fault and a clipping fault sound alike and read
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  differently. Pacing design: doc 11; `tools/audio-glitch-test.py`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`STALL=`, `CDAMP=`) reproduces both.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Driving a guest headless
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Wait for the guest, never a clock.** `tools/guestwait.sh` waits for a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  line our device wrote, QMP block stats going quiet, or a knock on the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Run dialog answered on COM1 (`docs/testing.md`, "Driving a guest").
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `BOOT_WAIT` and friends are caps on giving up. A screendump is never
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  evidence of life: `vga_draw_text` draws over a dead machine.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A frozen first frame with a blinking caret is a guest with no timer
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  interrupt**, not a hung emulator. `info registers` twice (EIP
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  unchanged), `info pic` (an unmasked `irr` bit with `isr=00`), `info
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  lapic` (`LVT0 masked`). Win98's restart was this (patch 22).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A "hung" Win98 desktop may be idle and unrepainted**: EIP moving with
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `HLT=1` across two `info registers`. `hang.txt` from
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `win98-game-test.sh` has it.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **XP's lazy writer holds small FAT writes for minutes.** A harness asks
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  COM1, not the scratch disk, whether a command finished.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Four shell traps that read as the test failing.** A `pgrep -f` /
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `pkill -f` pattern that appears in the calling command matches the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  wrapper (and `pkill` kills the session's shell); use `patter[n]`. Find
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  QEMU with `ps -C qemu-system-i386 -o pid=`, never `pgrep -x` (`comm` is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  truncated at 15 characters). A deep `OUT=` makes the QMP socket fail
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  with `AF_UNIX path too long` and the run does nothing. Editing a bash
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  script under a running instance breaks that instance.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **`grep -c` prints `0` and exits 1**, so `$(grep -c x f || echo 0)` is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `0\n0`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **The user's images are read-only.** Boot a qcow2 overlay or a copy;
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  while an overlay runs, the backing file is write-locked. Never run two
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  TCG guests at once on one box: they starve each other and a slow run
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  reads as a failure.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **End a Win98 run with the ACPI power button** (`system_powerdown`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Keystrokes die in a modal dialog, and a machine that does not power off
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  leaves the FAT dirty, so the next boot is safe mode: no driver, empty
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  logs, exactly like the thing under test failing. A failed boot does the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  same; let the safe-mode boot finish before trusting the next run.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Win98 runs under TCG, not KVM** (the family's default). Under `-accel
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  kvm` Explorer dies at start ("illegal operation", then *SHELL32.DLL is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  linked to missing export SHLWAPI.DLL:GetFileAttributesA*), so there is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  no Start menu to drive.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A bare `qemu-system-i386` has no 3D and opens no window.** QEMU is
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  built with no display, host-audio or extra backends (`configure-qemu.sh`,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the `no-optionals` check; `--disable-dsound` needed patch 23), so
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  pass-through is refused for want of a context provider. With no
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `-display`, QEMU starts a VNC server on `localhost:5900`. Look with
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `-display vnc=:0`, and play into `-audiodev none`.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A game that "freezes" is often showing a message box you cannot
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  see.** The player falls back to the VGA surface after 1 s without a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  presented 3D frame. Headless, `SHOTS=` in the game harnesses shows the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  box and `DRW_AFTER=` the stacks (XP).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A glitch shorter than a second shows only in the player's own
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  frames** (`PLAYER=1 PLAYER_SHOT_EVERY=6`). QMP screendumps come once a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  second, a headless console refreshes only when asked, and a screendump
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  shows the VGA surface, frozen while 3D presents.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An API's own answer is not evidence; the device's is.** `mcicda`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  answers `status mode` from the state it commanded (Win98 said
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  "stopped" while the drive played on), and a check that greps for a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  *line* proves nothing about the value in it. Ask the device (a trace)
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  and the output (the wav, the pixels), and assert the value.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Headless changes timing, not just output.** With no device window
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  DXVK's `Present` returns at once, so a busy-poll of a query starved the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  thread that had to answer it. Put `Sleep(1)` between polls. A cold
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  pipeline cache is the load that makes such a race show.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A game that runs far too fast is presenting, not timing.** Era titles
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  pace by `Flip`, so a flip that never blocks is a missing frame limiter.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `d3dpt-vga: N page flips in 5.0 s` is the guest's real frame rate; no
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  line means it blits to the primary. `DDFLAGS=32768` turns the vertical
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  blank off for the A/B.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **KVM `-cpu host` breaks Max Payne's level loading** ("Corrupt JPEG
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  data": a CPUID-dispatched decoder). `-cpu pentium3` under KVM works.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Prefer an era CPU model for games.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An XP game "crashes at startup" with `0xc0000142`**: a DLL of ours
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  returned FALSE from `DllMain`. Either qemu-3dfx's `OPENGL32.DLL` could
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  not open `\\.\MAPMEM` (FXPTL.SYS and the MAPMEM service missing:
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  install SETUP's Glide component as Administrator; OpenGL needs it
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  too), a `D3DPT\` DLL found no executor
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`D3DPT_STATUS_NO_EXEC`), or the protocol version differs (`d3dpt.log`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  names both).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A benchmark inside a DOS `.COM` keeps its data off the code page**,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  or self-modifying-code invalidation dominates the number.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Windows guests
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Win98 must be an ACPI install**, or PCI hot-adds (USB tablet, AC'97,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  NIC) are never seen and Device Manager shows "Plug and Play BIOS".
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Setup compares F000:FFF5 with 12/01/99; `prepare-qemu.sh` stamps the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  firmware 12/31/99 (the `bios-date` check), so a plain `SETUP` installs
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  ACPI. Repair an older image in Device Manager (`build-macos.md`), don't
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  reinstall.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **"Windows protection error" on `d3dpt-vga`, fine on the Cirrus.** A
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  display driver built before 2026-09-12 wants the register set exactly
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  and refuses a newer adapter, and `*DisplayFallback=0` leaves no VGA.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Boot on the Cirrus, run the ISO's `SETUP /ALL`, switch back. Newer
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  drivers accept any later register set (doc 15).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **`ExitWindowsEx` from a console program never returns on 9x** and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  holds the Win16Mutex; a worker thread makes it worse. Call it from a
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  process with no console: `SETUP` re-execs itself detached as `SETUP
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  /REBOOTNOW`. `rundll32 krnl386.exe,exitkernel` restarts with a dirty
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  FAT. The proof of a restart is a second SeaBIOS banner.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **Never overwrite a loaded 9x driver file in place.** KERNEL reloads
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  discarded segments from the new file at the old addresses. Stage it as
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `NAME.EX_` and rename it through `WININIT.INI` on the restart
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (`guest-tools/README.md`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An ISA device of ours must not sit on IRQ 9.** PIIX4 puts the ACPI
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  SCI there, so a line only the guest can lower is re-entered on every
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  `IRET` until #DF and a triple fault, which looks like a spontaneous
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  reboot. DOS masks IRQ 9 and never shows it. Prefer no interrupt line
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  where none is needed (doc 20 §5.1).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Devices and corruption
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **An interrupt a guest cannot acknowledge costs the line, not one
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  interrupt.** On the edge-triggered i8259 every later assertion into the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  held line is lost silently (doc 20 §5.2). `info irq` counts rising
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  edges, so a count that stops while the device still raises is this.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Diagnose with `-d trace:pic_set_irq`, `trace-event-set-state
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  memory_region_ops_write` over QMP for the ten seconds that matter, and
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the device's own status read from the monitor (`o` / `i`).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A crash that moves from victim to victim is memory corruption: A/B
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  the TCG switches before the driver.** Win98 dying in `SETUP` was patch
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  44's `uint16_t` TLB list wrapping, and one lucky control cost an
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  evening. Use `-accel tcg,<switch>=off` per patch, repeat every control,
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  and catch the reset with `-action reboot=shutdown,shutdown=pause -d
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  cpu_reset`, reading the blue screen out of VRAM.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- **A QMP medium change must pass `force`.** Without it a guest that
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  locked the tray (XP, for any open handle) gets an eject *request*, the
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  command is refused, and the swap lands whenever the guest lets go.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |### Where the subsystem traps live
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- The WGL rule (a `wgl*ARB` call with no context faults): doc 12.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- The Win98 display driver: the text page behind the linear frame buffer
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  (doc 19 §15), `pnpdrvr.drv` and the INF's `DelReg` (§16), `__loadds`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  and 32-bit register access (§14, §18), the DOS box's repaint (§29),
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  Mode X for 320×200 (§30).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- The XP driver's dxg rules, the DirectX 6 flip chain, untracked GDI
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  writes: doc 15.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- The CD drive: how each Windows stops a CD (doc 17 §5.4), and `libdisc`
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |  never asserting on a disc's values (doc 17).
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- Qt front end traps (bindings, dialogs, Esc, native styles): doc 07.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- x87 precision modes and the batteries: doc 13. SSE: doc 16.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- A slang preset smearing its edges (`clamp_to_border`): doc 03.
`package-windows.sh`, `package-msix.sh`, `win-run.sh`, `embed/mglcntx_embed.c`'s WGL half, `build-windows.md` | Done; the zip runs guests on the user's PC, and packs as a Store MSIX · Moto Racer's speed there, the native MSYS2 build and its ISO run, live control, the Store upload, the installer |- `macdeployqt` and the macOS bundle: `build-macos.md` "The app".
