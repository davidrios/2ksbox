74-no-glidept: the Glide pass-through device is not built (ADR-020)

qemu-3dfx's overlay wires two pass-through devices into the PC machine:
hw/3dfx (Glide, dispatching to a host libglide2x) and hw/mesa (OpenGL).
2ksbox retired the Glide pass-through in favour of the emulated Voodoo 2
(ADR-020, doc 21): no host wrapper is built or shipped, so the device
would only ever refuse. This takes hw/3dfx out of the build and the
machine while hw/mesa stays. The overlay directory is still copied in,
because qemu-3dfx's sign_commit stamps files in it; nothing compiles it.
The glide_* entries of patch 30's UI table stay as unreferenced defaults.

Drop: never; upstream will not grow a build option for one device.

diff --git a/hw/i386/pc.c b/hw/i386/pc.c
index bf92a76..1003147 100644
--- a/hw/i386/pc.c
+++ b/hw/i386/pc.c
@@ -1234,9 +1234,7 @@ void pc_basic_device_init(struct PCMachineState *pcms,
             ? ON_OFF_AUTO_OFF : ON_OFF_AUTO_ON;
     }
 
-    /* Glide pass-through */
-    glidept_mm_init();
-    /* MESA pass-through */
+    /* MESA pass-through (2ksbox: the Glide device is not built, ADR-020) */
     mesapt_mm_init();
     /* win98-xp-virt: paravirtual Direct3D (hw/d3dpt) */
     d3dpt_mm_init();
@@ -1268,14 +1266,6 @@ void pc_nic_init(PCMachineClass *pcmc, ISABus *isa_bus, PCIBus *pci_bus)
     rom_reset_order_override();
 }
 
-void glidept_mm_init(void)
-{
-    DeviceState *glidept_dev = NULL;
-
-    glidept_dev = qdev_new(TYPE_GLIDEPT);
-    sysbus_realize(SYS_BUS_DEVICE(glidept_dev), &error_fatal);
-    sysbus_mmio_map(SYS_BUS_DEVICE(glidept_dev), 0, GLIDEPT_MM_BASE);
-}
 void mesapt_mm_init(void)
 {
     DeviceState *mesapt_dev = NULL;
diff --git a/meson.build b/meson.build
index d777fb1..d31f799 100644
--- a/meson.build
+++ b/meson.build
@@ -3753,7 +3753,6 @@ if have_system or have_user
                          arguments: ['@INPUT@', '@EXTRA_ARGS@', '-o', '@OUTPUT@'])
   subdir('libdecnumber')
   subdir('target')
-  subdir('hw/3dfx')
   subdir('hw/mesa')
   subdir('hw/d3dpt')
   subdir('hw/voodoo')
